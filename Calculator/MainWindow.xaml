<Window x:Class="Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        DataContext="{Binding Source={StaticResource MainViewModelData}}"
        mc:Ignorable="d"
        Title="Calculator" Height="450" Width="800" MinWidth="500" MinHeight="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <!-- Menü sáv. (A MainViewModel a DataContext ennél az objektumnál.)-->
        <DockPanel Height="Auto" Grid.Row="0">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File">
                    <MenuItem Header="Kilépés" Command="{Binding CloseCommand}"></MenuItem>
                </MenuItem>
            </Menu>
        </DockPanel>

        <!-- Calculator View betöltés és a CalculatorViewModel beállítása neki DataContext-nek -->
        <ContentControl Grid.Row="1" Content="{Binding}" Style="{StaticResource MainContentControlStyle}" DataContext="{Binding CalculatorViewModelData}"></ContentControl>

    </Grid>
</Window>
